var util = {
    time: {
        toString: function(time){
            var date = new Date(time)
            return `${date.getYears()}-${date.getMonth()+1}-${data.getDate()} ${date.getHours()}:${date.getMinutes()}:${date.getSeconds()}`
        }
    }
}
